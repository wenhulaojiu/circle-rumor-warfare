# 圈层谣言攻防战 UI 设计规范

## 视觉方向

采用“编辑部报告 / 数据叙事”风格：深棕黑与暖象牙白交替作为主场景，搭配细线网格、超大衬线标题和少量荧光黄/洋红强调数据与行动。页面保持克制、留白充足，避免渐变堆叠和过度圆角。

## 配色

| 用途 | 色值 |
| --- | --- |
| 深色背景 | `#1C1012` |
| 深色面板 | `#281719` |
| 浅色背景 | `#F4F1EA` |
| 浅色面板 | `#EBE7DF` |
| 主文字（深色场景） | `#F8F5EE` |
| 主文字（浅色场景） | `#211617` |
| 次要文字 | `#AFA29A` |
| 暖黄色强调 | `#E9B949` |
| 洋红强调 | `#D94C86` |
| 青绿色状态 | `#5C9B83` |
| 分割线（深色） | `rgba(248,245,238,.22)` |
| 分割线（浅色） | `rgba(33,22,23,.18)` |

## 字体

- 标题：`Georgia`, `Songti SC`, `SimSun`, serif；页面主标题 42–64px，字重 400，行高 1.02。
- 区块标题：同上，24–34px，字重 400，行高 1.12。
- 正文：`Arial`, `PingFang SC`, `Microsoft YaHei`, sans-serif；14–16px，字重 400，行高 1.6。
- 标签与数据：无衬线 11–13px，字重 600，适当增加字母间距（`.08em`）。

## 间距

- 页面外边距：桌面 32px，移动端 20px。
- 模块间距：32–56px；卡片内部 20–28px。
- 元素间距：8px（紧密）、16px（常规）、24px（松散）。
- 采用 8px 基准网格，优先增加留白而不是堆叠信息。

## 组件样式

- 按钮：直角或 2px 圆角；主按钮暖黄色底、深棕文字；次按钮透明底、1px 细线；高度 42–48px，水平内边距 20–28px。
- 卡片：2–4px 圆角，1px 细边框；不使用明显阴影，仅允许 `0 8px 24px rgba(28,16,18,.08)`。
- 导航栏：透明或与背景同色，底部 1px 分割线；高度 64–72px。
- 输入框：透明底、底部或全边框 1px；聚焦时使用暖黄色描边。
- 标签：小字号、全大写/短中文，使用黄色或洋红色作为状态色，不依赖颜色单独传递信息。
- 图表：优先细线、横向条形和大数字；网格线低对比度，数据标签靠近数据本身。

## 布局原则

优先使用“标题 + 一句解释 + 大数字/主图 + 注释”的编辑型纵向节奏。主内容与辅助信息使用明显层级；深色和浅色区块可交替出现，形成滚动叙事。关系网络画布保留为核心视觉，但外围控制面板采用报告式排版。
